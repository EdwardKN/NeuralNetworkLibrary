plugins {
    id 'java'
}

group = 'se.klinghammer'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':src') // Correctly reference the src module
}

sourceSets {
    main {
        java {
            srcDirs = ['java'] // Adjust if necessary
        }
        resources {
            srcDirs = ['resources'] // Adjust if necessary
        }
    }
}
